# Non-sensitive configuration
ENVIRONMENT=development
APP_VERSION=1.0.0
PORT=5000
LOG_LEVEL=INFO
DEBUG=true

# Database settings (use secrets for credentials)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=myapp

# External services
API_TIMEOUT=30
CACHE_TTL=300